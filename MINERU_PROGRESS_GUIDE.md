# MinerU 实时解析进度条使用指南

## 功能概述

批量文件上传 + 一键入库时，**实时展示每个文件的 MinerU API 解析进度**，包括：

- 每个文件的独立进度条（0-100%）
- 实时进度描述（如"调用 MinerU API 中..."、"解压产物..."）
- 解析状态（parsing / done / failed）
- 重试次数和等待时间

## 使用方法

### 1. 选择文件
- 点击或拖拽多个文件到上传区域（最多 600 个）
- 支持格式：PDF / DOCX / DOC / PPTX / XLSX

### 2. 开启自动入库
- 打开"上传后自动入库"开关
- 这会触发 parse + chunk + dify 全流程

### 3. 上传并观察进度
- 点击"上传并入库"按钮
- **实时进度条**出现在上传区域下方
- 每个文件显示：
  - 文件名
  - 进度条（蓝色 → 绿色渐变）
  - 进度描述（如"调用 MinerU API（第 1 次）..."）
  - 进度百分比

### 4. 查看结果
- 解析完成后，进度条变为绿色（success）
- 解析失败时，进度条变为红色（exception）
- 下方表格展示每个文件的详细结果

## 技术实现

### 后端

1. **进度跟踪模块** (`app/services/parse_progress.py`)
   - 全局单例 `ParseProgressTracker`
   - 线程安全的内存字典存储
   - 自动清理 30 秒前的完成进度

2. **MinerU 客户端集成** (`app/services/mineru_client.py`)
   - 在 `parse_file()` 方法中调用 `tracker.update()`
   - 关键阶段更新进度：
     - 0%: 准备解析
     - 10%: 选择引擎（hybrid-engine / vlm-engine）
     - 20-65%: 调用 API（每次重试 +15%）
     - 70%: 解压产物
     - 100%: 解析完成
     - 0% + failed: 解析失败

3. **进度查询 API** (`app/api/parse_progress.py`)
   - `GET /api/parse/progress`
   - 返回所有文件的当前进度
   - 自动清理旧进度

### 前端

1. **轮询机制** (`BatchFileUpload.tsx`)
   - 上传时启动定时器（每 1 秒查询一次）
   - 调用 `getParseProgress()` 获取实时进度
   - 上传完成后停止轮询

2. **进度展示**
   - 使用 antd `Progress` 组件
   - 每个文件独立进度条
   - 渐变色：蓝色（#108ee9）→ 绿色（#87d068）
   - 状态：active（解析中）/ success（完成）/ exception（失败）

## 进度阶段说明

| 进度 | 阶段 | 描述 |
|------|------|------|
| 0% | 初始化 | "准备解析..." |
| 10% | 引擎选择 | "使用 hybrid-engine 引擎" |
| 20% | API 调用（第 1 次） | "调用 MinerU API（第 1 次）..." |
| 35% | API 调用（第 2 次） | "调用 MinerU API（第 2 次）..." |
| 50% | API 调用（第 3 次） | "调用 MinerU API（第 3 次）..." |
| 70% | 解压产物 | "解压产物..." |
| 100% | 完成 | "解析完成" |
| 0% + failed | 失败 | "解析失败（重试 N 次）：{错误信息}" |

## 注意事项

1. **并发解析**
   - 多个文件会并发调用 MinerU API
   - 每个文件的进度独立更新

2. **重试机制**
   - API 失败会自动重试（默认 3 次）
   - 重试等待时间：15s / 30s / 60s
   - 进度条会显示"第 N 次失败，Xs 后重试..."

3. **进度保留**
   - 解析完成后，进度保留 10 秒
   - 30 秒后自动清理（后端）

4. **性能影响**
   - 轮询间隔 1 秒，对后端压力小
   - 进度存储在内存，不写盘

## 故障排查

### 进度条不显示
- 检查浏览器控制台是否有错误
- 确认后端已启动 `/api/parse/progress` 接口
- 查看网络请求是否成功

### 进度卡住不动
- 检查 MinerU API 是否正常响应
- 查看后端日志 `data/logs/app.log`
- 确认文件是否在 pending/ 目录

### 进度显示但结果为空
- 检查 `data/parsed/{stem}/` 目录是否有产物
- 查看 MinerU API 返回的 ZIP 是否完整
- 确认后端 `_write_from_zip()` 是否成功解压

## 未来优化

1. **SSE 推送**（替代轮询）
   - 使用 Server-Sent Events 实时推送
   - 减少网络请求

2. **WebSocket 双向通信**
   - 支持取消解析任务
   - 支持调整优先级

3. **持久化进度**
   - 写入数据库或文件
   - 支持刷新页面后恢复进度

4. **详细日志**
   - 点击进度条展开详细日志
   - 支持下载日志文件
