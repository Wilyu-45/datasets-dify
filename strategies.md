| 策略 | 核心思想 | 优点 | 缺点 | 适用场景 | 本项目已应用 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **固定长度切分** | 按固定的字符数或Token数硬性切割。 | 实现简单、处理速度快，是很好的基准线。 | 语义破坏严重，极易从句子或段落中间切断。 | 日志文件、代码等结构化文本，或作为初步测试的基准方法。 | ✅ |
| **句子级切分** | 以句号、问号等标点符号作为分割点。 | 保持最自然的语义边界，符合人类阅读习惯。 | 单个句子信息量不足，可能把完整段落拆散。 | 普通文章、报告、FAQ问答等以句子为单位的文本。 | ✅ |
| **语义切分** | 利用AI模型计算句子间的语义相似度，在主题转变处切分。 | 语义完整性最好，每个块围绕一个主题。 | 计算成本高、速度慢。 | 对检索精度要求极高的专业领域，如金融、医疗报告等。 | ✅ |
| **递归切分** | 分层迭代切分，优先用大分隔符（段落），超长则用更小分隔符（句子）继续切分。 | 在语义完整性和块大小之间取得良好平衡。 | 实现相对复杂，需要预设分隔符优先级。 | 大部分RAG应用的默认推荐策略，适合处理结构复杂文档。 | ✅ |
| **基于文档结构切分** | 利用文档原有层级结构（如Markdown标题、PDF章节）切分。 | 保留文档逻辑结构，每个块对应完整章节，可溯源性强。 | 严重依赖文档本身的结构化程度，对纯文本无效。 | 产品说明书、技术文档、学术论文等有明确章节结构的文档。 | ✅ |
| **滑动窗口/重叠切分** | 切分时让相邻块有部分内容重叠。 | 防止关键信息（尤其边界处）被切断，保证上下文连贯。 | 增加数据冗余，提高存储和计算成本。 | 通常作为辅助策略，与其他方法（如固定长度）结合使用。 | ✅ |
| **父-子切分** | 先切分为大“父块”，再细分为小“子块”。检索用子块匹配，但将父块送给模型。 | 兼具检索的精准性和回答的丰富性。 | 实现复杂，对系统设计有更高要求。 | 需要精确检索但又依赖完整上下文的复杂问答场景。 | ✅ |
| **晚切分** | 先为长文本生成完整嵌入向量，再进行切分。 | 让每个chunk的向量感知更广泛的上下文，提升表征能力。 | 技术较新，对嵌入模型有特定要求，实现门槛高。 | 处理存在大量指代、歧义的复杂长文档。 | ✅ |
| **基于LLM的切分** | 利用大语言模型理解文档，自主决定最佳切分点。 | 理论上达到最优的语义理解和切分效果。 | 成本极其高昂、速度极慢，不适合大规模生产环境。 | 对质量要求极高的小规模文档处理，或作为研究探索。 | ✅ |

### 本项目已应用策略的具体说明

| 策略 | 在本项目中的具体应用 |
| :--- | :--- |
| **递归切分** | 以段落为最小分隔单元：段落长于目标长度（`chunk_target_chars`，默认 1500）时，按句号、分号、逗号等标点逐级切细直到满足长度约束；按标题层级的贪心合并由 `structure` / `hierarchical` 策略承担 |
| **基于文档结构切分** | 利用MinerU解析出的标题层级（`level=1`、`level=2`、`level=3`）和页面/块级结构进行切分 |
| **滑动窗口/重叠切分** | 长段落按句号切分时，添加100字符的overlap（上段末尾100字符重复到下段开头），保证上下文连贯 |
| **父-子切分** | 按标题层级切分时，父块包含完整章节内容，子块为各分段；检索时子块匹配，父块提供完整上下文 |

### 本项目实际实现的策略（chunk_strategies.py，共 8 个）

| 策略 key | 对应上表方法 | 实现说明 |
| :--- | :--- | :--- |
| `fixed` | 固定长度切分 | 按固定字符数硬切（`chunk_fixed_size_chars=800`，overlap `chunk_fixed_overlap_chars=100`） |
| `overlap` | 句子级切分 | 以句子为边界切分，长段落切分时添加 overlap（`chunk_overlap=100`） |
| `semantic` | 语义切分 | 按标题层级聚合段落，用句子向量相似度（`chunk_semantic_threshold=0.78`）识别语义转折点切分 |
| `parent_child` | 父-子切分 | 父块为章节上下文（`chunk_parent_size_chars=1500`），子块为检索单元（`chunk_child_size_chars=400`） |
| `structural` | 基于文档结构切分 | 利用 MinerU 标题层级（level=1/2/3）贪心合并段落，目标 `chunk_target_chars=1500`（默认策略） |
| `hierarchical` | 基于文档结构切分 | 按标题层级 1→4 逐级切分，最深层超长时按句子递归切细 |
| `llm` | 基于 LLM 的切分 | 调用大模型（OpenAI 兼容 `/chat/completions`）返回切分段落 JSON 数组，需配置 `llm_api_base_url` / `llm_api_key` / `llm_model`（默认关闭） |
| `embedding` | 晚切分 | 计算 chunk 嵌入向量后按语义切分（需配置 `chunk_embedding_api_url` / `chunk_embedding_api_key`，默认走 Dify 嵌入） |

### 关键参数与技巧

*   **块大小（Chunk Size）**：这是最关键的参数。块过大会包含过多噪音，过小则可能信息不足。普遍认为 **300-500 tokens** 是一个不错的起点，可参考模型上下文窗口和业务场景调整。
*   **重叠率（Overlap）**：建议设置 **10%-20%** 的重叠，确保关键信息不会因恰好落在切分边界而丢失。
*   **元数据（Metadata）**：为每个块附加文档来源、章节、标题等元数据。这能在检索时进行过滤，并让答案可溯源。
*   **特殊内容保护**：对于**表格、代码块、公式**等内容，应在切分时将其视为不可分割的整体，以保证其结构和语义的完整性。

### 选择建议

*   **通用文档，首选递归切分**：作为LangChain等框架的默认选择，它在大部分场景下表现均衡。
*   **结构化文档，善用结构切分**：对于Markdown、PDF等有明确标题层级和结构的文档，基于文档结构切分能最大程度保留语义。
*   **高精度场景，考虑语义切分**：当追求极致检索精度，且能接受较高计算成本时，语义切分是值得考虑的选项。
